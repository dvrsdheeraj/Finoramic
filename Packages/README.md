**Steps to follow**
<br>
#create virtual environment using `virtualenv` <br>
`virtualenv venv`
<br>
#to activate the virtual environment<br>
#For Ubuntu
`source venv/bin/activate` 
<br>
#For Windows
`venv\scripts\activate`
<br>
#Add packages.json file to install project dependencies
#for json formatt please see the package.json file in the folder

#now you are ready to install packages run <br>
`python packages.py`
<br>
